<% provide(:title, "パスワード再設定")　%>
<h1>パスワード再設定</h1>

<%= form_with(url: password_resets_path, scope: :password_reset,
        local: true) do |f| %>
    <%= f.label :email %>
    <%= f.email_field :email %>

    <%= f.submit "送信" %>
<% end %>
