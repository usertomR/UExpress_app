<%= form_with(model: current_user.article_bookmarks.find_by(article_id: @article.id),
              html: {method: :delete }, local: false) do |f| %>
  <%= f.submit "bookmark", class: "bookmark_now" %>
<% end %>