<%= form_with(model: current_user.nice_to_articles.find_by(article_id: @article.id),
              html: {method: :delete }, local: false) do |f| %>
  <%= f.submit "nice", class: "nice_now" %>
<% end %>