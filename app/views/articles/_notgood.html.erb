<%= form_with(model: current_user.nice_to_article_comments.find_by(article_comment_id: comment.id),
              html: {method: :delete }, local: true) do |f| %>
  <div><%= hidden_field_tag :article_id, article.id %></div>            
  <%= f.submit "good", class: "good_now" %>
<% end %>