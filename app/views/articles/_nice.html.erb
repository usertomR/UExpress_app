<%= form_with(model: current_user.nice_to_articles.build, local: false) do |f| %>
  <div><%= hidden_field_tag :article_id, @article.id %></div>
  <%= f.submit "nice", class: "not_nice_now" %>
<% end %>