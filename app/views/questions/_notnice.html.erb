<%= form_with(model: current_user.nice_to_answers.find_by(answer_to_question_id: answer.id),
              html: {method: :delete }, local: true) do |f| %>
  <%= hidden_field_tag :question_id, question.id %>
  <%= f.submit "nice", class: "nice_now" %>
<% end %>