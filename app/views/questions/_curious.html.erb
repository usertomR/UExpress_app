<%= form_with(model: current_user.curious_questions.build, local: false) do |f| %>
  <div><%= hidden_field_tag :question_id, @question.id %></div>
  <%= f.submit "気になる", class: "not_curious_now" %>
<% end %>