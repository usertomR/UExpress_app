<%= form_with(model: current_user.question_bookmarks.find_by(question_id: @question.id),
              html: {method: :delete }, local: false) do |f| %>
  <%= f.submit "bookmark", class: "bookmark_now" %>
<% end %>